var Player = require('logic/Player/Player');

var PlayerFactory = function(treasury) {
    var player = new Player();
    player.receiveMoney(treasury.getMoney(3));
    player.receiveResources(treasury.getResources({
        sand: 1,
        wood: 1
    }));
    return player;
};

module.exports = PlayerFactory;
