var _ =require('underscore'),
    utils = require('utils'),
    Resource = require('/logic/resources/Resource'),
    Sand = function() {

    };

utils.inherit(Sand, Resource);

_(Sand.prototype).extend({
    type: 'Sand'
});

module.exports = Sand;
